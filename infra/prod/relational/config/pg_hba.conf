# Chi può connettersi e come.
# TYPE  DATABASE  USER  ADDRESS       METHOD
local   all       all                 trust            # socket unix nel container (init interni)
host    all       all   127.0.0.1/32  scram-sha-256
host    all       all   10.0.1.0/24   scram-sha-256    # sola subnet privata: coordinator↔worker + driver
